<?xml version="1.0" encoding="utf-8"?>
<MotionScene xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:motion="http://schemas.android.com/apk/res-auto">
    <Transition
        motion:constraintSetStart="@+id/start"
        motion:constraintSetEnd="@+id/end"
        motion:motionInterpolator="linear">
    </Transition>

    <ConstraintSet android:id="@+id/start">
        <Constraint
            android:id="@+id/mission_suggest_progress"
            android:layout_width="20dp"
            android:layout_height="20dp"
            motion:layout_constraintStart_toStartOf="@id/mission_suggest_progress_1"
            motion:layout_constraintEnd_toEndOf="@id/mission_suggest_progress_1"
            motion:layout_constraintTop_toTopOf="parent" />
    </ConstraintSet>
    <ConstraintSet android:id="@+id/end">
        <Constraint
            android:id="@+id/mission_suggest_progress"
            android:layout_width="20dp"
            android:layout_height="20dp"
            motion:layout_constraintStart_toStartOf="@id/mission_suggest_progress_6"
            motion:layout_constraintEnd_toEndOf="@id/mission_suggest_progress_6"
            motion:layout_constraintTop_toTopOf="parent" />
    </ConstraintSet>
</MotionScene>